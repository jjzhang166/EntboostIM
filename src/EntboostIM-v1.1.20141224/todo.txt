1、群组成员相关
  （列表展示、打开对话框）-（加载）
  
2、注册登录、聊天等测试


3、界面修改
 （主界面风格，按钮风格）-多余的图片清理，完整的界面设计
 还有一些按钮风格需要修改
 
4、讨论组
--------------
接收讨论组信息
1、接到1对1会话转讨论组的信息eb_sign_fu_msg
--------------

5、服务器自动应答

6、扩展应用接口

7、缓存保存、加载离线消息，保存等
8、搜索
（本地搜索，可能还有数据未加载）
9、代码结构调整
10、头像



buglist:
1、再次登录，出现eb_u_onlineKEY错误
  （修改了底层数据格式，导致一些代码判断上出现错误）
  还是存在同样的现象。。。。
  sessionid在发送协议的时候为空，当网络延迟重发的时候，就会出现服务器判断为多个session的情况，导致多次返回
  
2、企业群组的本地存储重复了两次
（添加群组的时候做了同步，问题不再重现）
----又重现了，感觉不像重复存储的问题
----确实是重复了，又出现了，感觉是同步的问题

3、500成员的群组成员无法加载
（有些成员的状态可能不是离线，添加成员的时候做了同步）

4、如果不加载群组信息，再登录后就不会默认从服务器中读取数据
(保存用户信息时，版本数据被覆盖了)

5、企业信息偶尔会加载为空

6、设置服务器地址，如果连不上需要重新设置 

7、群组成员默认没有加载，导致发消息的联系人名称没有显示

8、退出系统时抛空指针